# Legacy asset directory

The current manifest uses `public/assets/sculptures/`. This older empty location
is retained only so existing local checkouts are not disrupted; do not add new
study assets here.
